package org.nudge.elasticstack.context.elasticsearch.builder;

import org.junit.Test;
import org.nudge.elasticstack.context.nudge.dto.LayerDTO;
import org.nudge.elasticstack.context.nudge.dto.TransactionDTO;

import java.util.ArrayList;
import java.util.List;

public class TransactionSerializerTest {

	@Test
	public void test_compute_layers_time() throws Exception {
	}

	@Test
	public void test_build_layers_from_transaction() throws Exception {
		// given
		List<TransactionDTO> transactionDTOS = new ArrayList<>();
		TransactionDTO transactionDTO = new TransactionDTO();
		List<LayerDTO> layerDTOS = new ArrayList<>();
		LayerDTO layerDTO = new LayerDTO();
//		layerDTO.s
//		transactionDTO.setLayers();

		// when

		// then

	}

}